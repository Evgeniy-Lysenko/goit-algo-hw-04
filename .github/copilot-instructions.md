# Copilot instructions for this repository

## Краткое описание 🧭

- Это набор учебных скриптов на Python (домашние задания). Основные темы: работа с файлами (чтение/запись, `pathlib`), регулярные выражения, даты (`datetime`), генерация случайных чисел (`random`).
- Проекты не являются пакетом — это отдельные исполняемые скрипты: `homework*.py`, `homework*_m4.py`, `work_with_file_structure.py` и т.д.

## Быстрая старт-инструкция ▶️

- Запуск: `python3 homework1_m4.py` (или `python homework1.py` в другом каталоге). Скрипты используют `input()` — запускайте интерактивно или подавайте входные данные через перенаправление.
  - Пример: `python3 homework1_m4.py` → введите `User_salary.txt` когда появится запрос.
- Нет `requirements.txt` и внешних зависимостей — используется только стандартная библиотека.

## Формат файлов и примеры (важно) 📄

- `User_salary.txt` — каждая строка: `name,salary` (UTF-8). Пример:
  - `Alex Korp,3000`
- `cat_info.txt` — каждая строка: `id,name,age`. Пример:
  - `60b90c1c13067a15887e1ae1,Tayson,3`
- Скрипты ожидают текст в кодировке UTF-8 и разделение запятой без дополнительных кавычек.

## Частые паттерны и конвенции 🔍

- Функции именуются в snake_case и обычно принимают путь/строку как параметр (например, `total_salary(path)`).
- Скрипты часто: определяют функцию и затем вызывают её в глобальной области (т.е. `result = fn(...)`, `print(result)`).
- Комментарии местами на украинском/английском — учитывайте при объяснениях и локализации сообщений.

## Что важно при внесении изменений ✍️

- Когда добавляете тесты, избегайте интерактивного `input()` в тестируемой логике: вынесите ввод/вывод в отдельный слой и тестируйте чистые функции напрямую.
  - Пример: вместо тестирования `python script.py` — импортируйте `from homework1_m4 import total_salary` и вызовите `total_salary('tests/fixtures/User_salary.txt')`.
- Обработка ошибок минимальна (например, `FileNotFoundError` в одном файле). При изменениях сохраняйте явную и простую обработку ошибок и возвращаемые значения (tuple `(total, average)` и т.п.).

## Отладка и типичные команды 🐞

- Интерактивный запуск: `python3 homework2_m4.py`.
- Запуск с тестовой фикстурой: `python3 -c "from homework1_m4 import total_salary; print(total_salary('User_salary.txt'))"`
- Отладка через pdb: `python3 -m pdb homework1_m4.py`.

## Безопасность, совместимость и окружение ⚠️

- Целевая версия Python: Python 3 (рекомендуется 3.8+). Никаких внешних зависимостей.
- Работа с файлами предполагает доверенные локальные файлы — не обрабатывайте неизвестные бинарные входы без проверки.

## Где смотреть, если нужно понять реализацию 📚

- Основные точки входа/файлы: `homework1.py`, `homework2.py`, `homework3.py`, `homework1_m4.py`, `homework2_m4.py`, `homework3_m4.py`, `work_with_file_structure.py`.
- Примеры данных: `User_salary.txt`, `cat_info.txt`.

---

Если нужно, могу сократить/расширить разделы, добавить конкретные примеры тестов или шаблоны PR — скажите, какие части стоит уточнить.
