# Домашні завдання по роботі з файлами в Python

Репозиторій містить рішення домашніх завдань, присвячених роботі з текстовими файлами, обробці даних та базовій обробці винятків у Python.

---

## Домашнє завдання 1. Аналіз заробітних плат

Реалізована функція `total_salary(path)`, яка аналізує текстовий файл з інформацією про заробітні плати розробників.

### Формат файлу
Кожен рядок файлу містить ім'я розробника та його зарплату, розділені комою без пробілів:


### Функціональність
Функція:
- зчитує дані з файлу;
- обчислює загальну суму заробітних плат;
- обчислює середню заробітну плату.

### Результат
Функція повертає кортеж із двох значень:
(total_salary, average_salary)


### Особливості реалізації
- використовується менеджер контексту `with`;
- явно задано кодування файлу;
- передбачена обробка помилок (наприклад, якщо файл відсутній).

---

## Домашнє завдання 2. Інформація про котів

Реалізована функція `get_cats_info(path)`, яка зчитує файл з інформацією про котів та повертає дані у вигляді списку словників.

### Формат файлу
Кожен рядок містить:
id,ім'я,вік


### Функціональність
Функція:
- зчитує файл;
- для кожного кота формує словник з ключами `id`, `name`, `age`;
- повертає список таких словників.

### Результат
Приклад повернених даних:
[
{"id": "60b90c1c13067a15887e1ae1", "name": "Tayson", "age": 3},
{"id": "60b90c2413067a15887e1ae2", "name": "Vika", "age": 1}
]


### Особливості реалізації
- безпечне читання файлу за допомогою `with`;
- обробка можливих винятків;
- чиста та зрозуміла структура коду.

---

## Домашнє завдання №3

Скрипт для візуалізації структури директорії з кольоровим виведенням файлів та піддиректорій.

### Вимоги
- Python 3.x
- Віртуальне оточення
- Бібліотека `colorama`

### Встановлення

python -m venv .venv
source .venv/bin/activate      # Linux / macOS
.venv\Scripts\activate       # Windows

pip install -r req_homw3_m4.txt

### Запуск
python main.py <шлях_до_директорії>

### Особливості реалізації
піддиректорії відображаються синім кольором;
файли відображаються зеленим кольором;
обхід директорій реалізовано рекурсивно;
залежності ізольовані у віртуальному оточенні.